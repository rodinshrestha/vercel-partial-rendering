import React from 'react';

import OrderAccordion from '../OrderAccordion';

const OrderLoading = () => {
  return <OrderAccordion data={null} searchParams={{}} loading />;
};

export default OrderLoading;
